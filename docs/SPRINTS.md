# SPRINTS.md — Aurora Program — Step-by-step development plan
*Every sprint ends in a playable build. No sprint adds systems outside the GDD without updating the GDD first. Development Phase 1 = Sprints 0–8 (through the first successful launch). No monetization code anywhere in v1.*

**v2.1 changes:** Sprint 0 adds the headless balance simulator (the "Phase 0 simulation" ECONOMY_MODEL references now exists as a deliverable); Sprint 2 adds the dev-only time-warp; Sprint 1 covers insolvency; Sprint 7 implements roll commitment; Sprint 8 closes Phase 1 with a **private** playtest build (public playtest stays in Sprint 12); Sprint 12 defines the telemetry endpoint decision.

## Sprint 0 — Foundations (2–3 days)
1. Scaffold: Vite + React + TS + Zustand. ESLint + Prettier + Vitest.
2. `state/persistStore.ts`: localStorage save/load, `schemaVersion: 1`, migration registry, autosave 10 s + on close.
3. `core/economy.ts`: `costAtLevel`, `productionPerSecond` + unit tests against ECONOMY_MODEL §4.
4. `data/buildings.ts`: all **18** buildings + internal upgrades transcribed (stable ids: `finance`, `vab`, `launchRail`, `padB`…).
5. **`sim/run.ts` — headless balance simulator (dev-only, env-gated):** runs the economy with a simple bot policy (build cheapest useful thing, keep salaries paid, fly when possible) at accelerated time; outputs a day-by-day CSV (resources, income/expense, salary ratio, research income split lab-vs-Flight-Data). **Three profiles: `optimal` (upper bound), `human` (3×20 min sessions/day — the profile all targets are stated against), and `casual` (one ~30 min session/day — the realistic once-daily player who will hit the 16 h offline ceiling every day; instrumentation only, reports lost production).** This is the tool that verifies ECONOMY sanity rules (salaries 30–55% at 5 checkpoints; Flight Data 20–35% of Research income in flight eras; **pacing floor: human must not reach Aurora I before day 5; pacing CEILING: flag if human exceeds day 12** — too slow is as much a failure as too fast). Re-run whenever ECONOMY values change.
6. UI shell: top ticker (4 resources + rates), complex tabs (Campus only active), number formatting (§12).
**Acceptance:** app loads, persists across reload, shows Funding 0 and the pitch button; `sim/run.ts` produces a coherent 3-day CSV.

## Sprint 1 — Economic core (3–4 days)
1. Manual actions (pitch, 1 s cooldown) with floating +10 feedback.
2. Timestamp-based game loop (`requestAnimationFrame` + delta; NEVER accumulating setInterval) in `core/tick.ts`.
3. Build/upgrade Campus: Offices, Finance, Quarters, R&D Lab.
4. Staff: hire Technicians, assign/unassign slots, per-second salaries, Quarters cap.
5. **Insolvency (GDD §1b):** Funding 0 with salaries due → staffed production pauses, `payrollUnpaid` flag drives the ticker banner; auto-resume on payment. Unit tests in `core/economy.ts`.
6. Resource caps (pre-Warehouse): production halts at cap with amber ticker warning; one-time payments exceed caps per GDD §1c (test both).
**Acceptance:** pitch→hire→assign→passive Funding loop works; salary burn visible; letting Funding hit 0 pauses production and recovers via pitching; reload neither loses nor duplicates resources.

## Sprint 2 — Time engine & offline (3–4 days)
1. `core/time.ts`: process queue `{id, kind, startedAt, durationMs, payload}`; parallel processes; timestamp resolution.
2. Offline calc on open: resources **and salaries** at 60%, 10 h cap; processes at 100%; insolvency resolved with the same online logic; "While you were away" screen with breakdown (including any payroll stoppage window).
3. **Dev time-warp (dev builds only, env-gated):** global time multiplier ×1 / **×5** / ×60 / ×600 applied at the timestamp layer, so offline, processes and economy all accelerate consistently. Without this, multi-hour timers are untestable. **×5 added for the owner's own manual playtesting sessions** (fast enough to skip dead air, slow enough to still feel and evaluate the game's real pacing — ×60/×600 are for verifying timer math, not for playing). Still dev-only/build-excluded like the rest of this tool; whether real players ever get an official speed control is a separate, later question (parked in BACKLOG, not decided now).
4. Process UI: progress bar + remaining time on building tiles.
5. Telemetry middleware (`state/telemetry.ts`): FTUE funnel events to exportable local buffer.
**Acceptance:** close 1 h and return shows correct summary (clock-manipulation test); two parallel processes resolve correctly; a full simulated day runs in <3 min at ×600.

## Sprint 3 — Full production (3–4 days)
1. Complex B: Supply Depot, Fabrication (consumes Materials per Hardware; produces at current tier into `byTier`), Refinery, Warehouse, Propellant Depot.
2. Input-starved buildings pause with indicator (never negative production).
3. Manual gathering + Rush Order action.
4. Complex B unlock by lifetime Funding ≥ 300 (lifetime counter, not balance).
5. Hardware tiers as first-class state (`byTier`, Aluminum only until Titanium tech); cost checks support `minTier`.
**Acceptance:** Materials→Hardware and Materials→Propellant chains run with their own caps; starvation pauses visible; tier bookkeeping invariant (sum(byTier) === amount) holds under tests.

## Sprint 4 — Research (2–3 days)
1. `data/research-tree.ts` per ECONOMY_MODEL §5 (deps, cost, duration, declarative modifier effects) — including Remote Ops (offline cap 16 h).
2. Research as timed process (one node at a time in v1).
3. Research panel: branches, locked/available/in-progress/done states.
4. Central modifier system (`core/modifiers.ts`): every bonus source registers modifiers; systems query them — NEVER hardcode a bonus inside a system.
5. Role promotions (Classroom upgrade) as timed process.
**Acceptance:** Basic logistics reduces transfer 25% via modifier; Remote Ops raises the offline cap via modifier; save/load preserves in-progress research.

## Sprint 4.5 — Presentation conventions (1–2 days, UI only)
*Owner of the v3.0 doc changes (ECONOMY §12, UI_SPEC §2c/§4). Runs BEFORE Sprint 5's UI work so nothing is built twice against the old convention. No core/data/economy changes — presentation only.*
1. **Cost & amount rendering (UI_SPEC §4, replaces the v2.8 abbreviation rule):** costs render as icon + number with no resource noun; Funds render as `$400` / `$1.25M`; full names only in ticker, tooltips, staff/economy panels and reward summaries; every icon gets a tooltip/long-press with its full name. Retrofit every existing screen (tiles, hire buttons, upgrade buttons, research panel, away modal, ticker).
2. **Active process strip (UI_SPEC §2c):** persistent strip under the ticker; NO process may exist without a chip (the promotion timer being untrackable is the bug that motivated this). Label + remaining time + progress fill, soonest-first, tappable, collapses to zero height when empty, "+N" overflow past 4.
3. **Staff availability chip in every complex tab header** (unassigned pool only), tappable to the staff panel.
**Acceptance:** a promotion is trackable end-to-end from the strip; the Production tab shows correct availability without navigating to Campus; no price tag anywhere renders a resource noun (Playwright-verified).

## Sprint 5 — Testing & designed failure (3 days)
1. Complex C: Engine Test Stand (build + slots).
2. Certifications as processes (§6), including extended certification for every engine type.
3. **Scripted failure**: first Probe-1 test always fails → recover 6 H, +30 XP, +100 Flight Data, narrative N-07, unlock retry.
4. Mission Log: feed component (collapsible bottom panel) + `data/narrative.ts` with milestone triggers.
5. Flight Experience visible as a resource (no trees yet). Flight Data flowing into Research per §8 — verify the lab-vs-Flight-Data split in `sim/run.ts` output.
**Acceptance:** full test-fail-narrative-retry-certify flow works and is narrated.

## Sprint 6 — Sounding rockets: the first launches (3 days)
1. Launch Rail (+ Extended Rail upgrade) and sonda assembly at the Test Stand workshop (ECONOMY §7a).
2. S-1 mini-checklist (3 items) + simplified Confidence (base 65) + roll commitment at checklist completion + countdown + results — the full launch loop in miniature.
3. Tier-0 sounding-payload contracts (1 offer, 6 h rotation; all-inclusive cost per §10): the early financial engine.
4. S-2 + Kármán line record + beats N-08b/N-08c.
**Acceptance:** repeatable S-1 campaign funds progress via tier-0 contracts; extended certification reaches 100% Confidence; S-2 crosses Kármán and awards its record.

## Sprint 7 — VAB, Aurora I & full Launch Sequence (4 days)
1. Complex D: VAB, Pad, Launch Control, Tracking Station (build, slots, unlock via Flight program tech). Mission state is per-pad (`pads.padA`) from the start.
2. Aurora I stage integration incl. satellite payload (§7): sequential timed stages; rocket state machine; propellant loading (T-08 depot-capacity tooltip).
3. Full 8-item Launch Sequence screen + `core/confidence.ts` (GDD §7b) with visible breakdown; Orbital-1 probabilistic certification + its extended certification.
4. **Roll commitment:** `committedRoll` drawn and persisted at checklist completion; countdown resolves it deterministically (export/import cannot re-roll — regression test).
5. Countdown → resolution → results with rewards + Records; failure resolution (60% Hardware recovery, 80% success-XP, half-time re-integration, no Rep/payout).
**Acceptance:** end-to-end first satellite launch; confidence matches formula and 100% is reachable without XP; sonda loop and full loop coexist.

## Sprint 7.5 — Discovery & clarity pass (2–3 days, presentation + one new capability)
*Owner played the real build end-to-end for the first time and found that §2b's progressive-disclosure rule was never given exact per-building conditions for Campus, plus several real content gaps (missing effect text) and one missing capability. No economy values change except two items explicitly marked SCOPED UNLOCK below.*
1. **Campus staged reveal (UI_SPEC §2d):** implement the exact 4-step sequence — Offices only → Finance at 150 lifetime Funding → Staff panel at Finance built → Crew Quarters + R&D Lab at first 2/2 staff cap. Research panel hidden entirely until R&D Lab is built.
2. **Research panel redesign (UI_SPEC §3):** compact vertical tree per branch (small connected nodes, states by fill, detail-on-tap side/bottom panel) replacing the always-expanded card grid.
3. **Clarity rules (UI_SPEC §4):** capped-resource tap names the building that raises it; consuming buildings show inputs as their own line (ECONOMY §4 Fabrication/Refinery updated); upgrade buttons preview the next level's delta; zero-effect nodes say so explicitly; tier-change toast (NARRATIVE T-14).
4. **Missing player-facing text (NARRATIVE §8/§9/§10):** all research node descriptions, Rush Order, and Testing/Launch complex intro tooltips (T-16/T-17).
5. **Staff dismissal (UI_SPEC §4b, new capability):** Release action per pool member, no refund, salary stops immediately. Promotion cost-advantage hint in the staff panel (surfaces existing math, no value change).
6. **SCOPED UNLOCK, re-run sim after:** Engine Test Stand's per-level effect (−3%/level certification duration) and Auto-refuel's defined effect (−50% satellite propellant loading) were previously undefined gaps, now specified in ECONOMY §4/§5 v3.5. Implement, then re-run the full sweep — pacing floor (day 5) / ceiling (day 12) and salary band (30–55%) must still hold.
**Acceptance:** a fresh save shows only Offices for the first ~2 minutes of clean play, reaching Finance/Staff/Quarters+Lab in the documented order; the research tree fits without scrolling past 2-3 screens on desktop; every building/upgrade/research node states its effect; Test Stand leveling and Auto-refuel visibly do something; staff can be released; sim sweep still green.

## Sprint 8 — FTUE & Phase 1 close (3 days)
1. Opening sequence (GDD §11): first decision <30 s, contextual one-time tooltips (NARRATIVE §2).
2. Locked complexes greyed with visible conditions; milestone call-outs (small non-blocking modal).
3. Polished "While you were away" (incl. payroll-stoppage reporting); designed session-1 ending (a timer always left running).
4. Settings screen (UI_SPEC §6): save export/import (string + file), manual save, hard reset, sound/motion toggles. Import validates version and rejects corrupt saves with a clear message.
5. Funnel verification via telemetry; balance pass = `sim/run.ts` day-by-day output vs the session table (including the sonda campaign days). **Economy unlock window: this is where the parked Campus/Production upgrade set (BACKLOG) gets priced and implemented — including the slot-adding upgrades that fix the idle-staff problem, the R&D Lab's parallel research track, AND Aluminum alloys' contingent Fabrication bonus (contingency met by real playtesting — Materials branch felt thin). Re-run the full sweep (all three profiles) afterwards; every sanity rule must still hold.**
6. QA: save/load regression on every system; offline with every process kind; export→wipe→import round-trip test.
7. **Private playtest build:** deploy to itch.io as a private/unlisted build for 3–5 trusted testers with the feedback form. The game's #1 risk is multi-day pacing, which cannot be validated solo — do not wait until Sprint 12 for external eyes.
**Acceptance (END OF PHASE 1):** a fresh tester reaches the first successful SATELLITE launch (Aurora I) with no external explanation, having flown multiple sondas along the way, over a multi-day arc.

## Sprint 9 — Contracts & events (3–4 days)
1. Payload Processing + `core/contracts.ts`: generator (2 offers, 8 h rotation, §10 templates), pad queue shared with story missions.
2. Full contract cycle: accept → build payload → checklist → launch → pay/deadline fail (−15 Rep, floor 0). Tier-2 requires Titanium-tier Hardware via `minTier`.
3. **Launch Pad B**: buildable after Aurora I success + Rep ≥ 40 (ECONOMY_MODEL §4). Adds `pads.padB` — **no schema migration needed** (per-pad state since Sprint 7). The scarcity phase (one pad) must be felt first; Pad B is the earned relief and the "program is growing" beat (narrative N-17).
4. Random events (`core/events.ts` + NARRATIVE §3): 15% check per 10 min, always 2 options, never during countdown; E-04's salary premium implemented as a modifier.
**Acceptance:** pad-queue tension works; deadlines penalize correctly; private-playtest feedback from Sprint 8 triaged into fixes or BACKLOG. **Explicit tester question to ask (v1's known thin spot): after Aurora I, does the remaining arc — 2 contract tiers, Pad B, events, XP nodes — sustain interest, or does the game feel finished at the satellite?** A "feels finished" answer is a v1 scope signal, not a bug: the fix would be pulling one BACKLOG item forward, decided with data.

## Sprint 9.5 — Second discovery & clarity pass (2–3 days, presentation + one pacing fix)
*Owner played a fresh build end-to-end again and found: Campus/Production's staged reveal has real gaps, several UI copy issues (redundant text, wrong/generic messaging, a real rate-rounding display bug), the research tree layout still didn't land, missing feedback on flights and completions, and one real pacing regression (Basic engineering too cheap, undermining the promotion-bootstrap design). No economy values change except the one item explicitly marked SCOPED UNLOCK.*
1. **Training Center timing (UI_SPEC §2d):** moves from "visible from game start" to appearing alongside Crew Quarters + R&D Lab (first 2/2 staff cap) — still the only v1 teaser, just later.
2. **Production staged reveal (UI_SPEC §2e, NEW):** Supply Depot only at complex unlock → Fabrication + Warehouse together once Supply Depot is built → Refinery + Propellant Depot together once "Sounding rockets" tech is researched.
3. **Copy & display fixes (UI_SPEC §4, NARRATIVE §7/§9):** drop the redundant "(currently X)" from upgrade-delta previews; "fully staffed" now names the real slot-adding upgrade when one exists (T-12a/T-12b) instead of a generically wrong line; idle-hire warning shortened (T-11), replaced by a permanent per-role salary line in the hire panel (T-13); rate/delta display fixes the real "+0.0/s" rounding bug (values below 0.1 get 2 decimals — ECONOMY §12) — this was hitting R&D Lab's 0.03/level rate, audit Fabrication/Refinery too since they're close to the boundary.
4. **Mission Log (UI_SPEC §2f):** unread badge on the collapsed header; promotions/research/upgrades now log a line on completion (T-18/T-19/T-20) — previously silent.
5. **Research tab (UI_SPEC §2g, §3):** moves to its own top-level tab; redesigned as horizontal per-branch lanes (Design B, selected over a radial "orbit map" — mobile-first requirement decided it), replacing the v1.1 compact-tree attempt that still didn't satisfy the owner.
6. **Sonda flights get real feedback (UI_SPEC §3 screen 5, NARRATIVE T-21/T-22):** S-1/S-2 now get the SAME full-screen countdown+result takeover Aurora I already has (previously ending in an easy-to-miss log line), plus a one-line purpose blurb on the checklist screen explaining why the player is launching and what they get.
7. **Verify the Sprint 8 Warehouse "Inventory system" upgrade is actually discoverable and working** — the owner reported no way to expand capacity; this upgrade should already exist. If it's genuinely broken, fix it; if it's just poorly surfaced, item 3's dynamic "fully staffed" copy should already resolve it — confirm live either way.
8. **Dev time-warp gains a ×5 preset** (×1/×5/×60/×600) for the owner's own manual playtesting — still dev-only/build-excluded.
9. **SCOPED UNLOCK, re-run sim after:** Basic engineering's cost raised 15R/3min → 120R/45min (ECONOMY §5 v3.7) — at the old cost it was reachable almost immediately, letting direct Engineer/Scientist hiring skip the promotion-bootstrap pacing the game has repeatedly defended (E-04's precondition, the day-5 floor). Re-run the full sweep (all three profiles); report the usual table. If the floor/ceiling breaks, report — don't retune further yourself.
10. **SCOPED UNLOCK, re-run sim after (v3.8, added after this sprint's own findings — fold in if not yet closed, otherwise treat as an immediate follow-up):** building expansion milestones (ECONOMY §4c) — every slotted building gains +1 slot per role it already employs every 10 levels, universal and automatic, coexisting with the existing specific slot upgrades. Fixes the "+1 slot upgrade feels like a non-event" concern with an infrequent, celebrated milestone (T-23) instead of either constant per-level growth (rejected earlier — forces mandatory headcount every level) or purchase-only growth. Re-run the sweep once more after this addition; if any building's milestone timing meaningfully disturbs the salary band, report the specific building/level.
11. **Disclosure audit — Testing and Launch complexes.** The same "everything shows at once" bug was found twice now (Campus in 7.5, Production in this sprint) — that's a pattern, not a coincidence. Confirm live whether Testing (Test Stand, Launch Rail, Payload Processing) and Launch (VAB, Pad, Control, Tracking, Pad B) have the same problem. If they do, apply the same staged-reveal treatment (natural candidates: Launch Rail before Test Stand's internal upgrades show; Payload Processing already gates on Aurora I success per GDD; VAB/Pad/Control before Tracking Station and Pad B). Report findings even if clean.
12. **Current Directive (UI_SPEC §2h, NARRATIVE §11, NEW):** a persistent, dynamic "what now" indicator near the ticker — addresses a confusion pattern that recurred across multiple unrelated features in playtesting (why launch this, what does this do, what's next), traced to one root cause rather than many separate ones. Extends the FTUE tooltip system into an ongoing feature. Ship the D-01 through D-12 directives from NARRATIVE §11 as the initial set; the pattern extends naturally to later content.
13. **Two narrative beats (T-24/T-25, genre-research-informed):** a Confidence explainer the first time it shows below 100% (teaches the golden rule — 100% is always reachable — at the moment it matters), and a distinguishing beat before Aurora I's VAB integration begins, framing it as the program's first ORBITAL attempt versus routine sonda flights.
14. **Progressive rocket blueprint assembly (UI_SPEC screen 4, NEW — "something real to look at"):** during VAB integration, the blueprint SVG builds up visually stage by stage (empty outline → Structure → Engines → Guidance → payload → final integration) instead of appearing complete only at the end. Reuses the already-planned blueprint art technique (§1b); no new art pipeline. Applies to Aurora II once it exists.
**Acceptance:** Production reveals in the documented staged order on a fresh save; every upgrade/rate display reads correctly (no "(currently X)" redundancy, no "+0.0/s"); Research is its own tab, laid out as lanes; a promotion produces a visible log entry; an S-1 flight ends in a full-screen result, not just a log line; Warehouse capacity growth is confirmed working; sim sweep still green.

## Sprint 10 — XP trees & orbital mission (3 days)
1. Flight Experience trees (§9) via modifier system; mechanic-changing XP nodes (Partial reusability, Parallel integration — ECONOMY §9; NOT the research tree's "VAB queues" node, which shipped in Sprint 4) with dedicated logic.
2. Tracking Station + XP multiplier; Orbital flight tech; Aurora II (probabilistic Orbital-1 cert, mission-2 requirements).
3. v1 ending screen: orbital milestone + crewed-program teaser.
**Acceptance:** full v1 arc playable; every XP node produces its measurable effect.

## Sprint 10.5 — Visual Identity 2.0 (2–3 days, presentation only)
*Owner's call: the game's current visuals are too flat/text-heavy, and after seeing the progressive rocket blueprint idea land well, asked to commit further rather than treat it as a one-off. Four features selected from a proposed set of five; all are pure presentation — zero economy impact, no scoped unlock, no sim re-run needed. Placed after Sprint 10 because the Constellation View needs Aurora II and satellite contracts to exist to show real variety.*
1. **Confidence Dial (UI_SPEC screen 4):** replaces the plain Confidence percentage with a radial gauge — a blueprint-style needle sweeping a 0–100 arc, each formula term rendering as its own colored arc segment that fills in as satisfied. Tap still opens the existing numeric breakdown; the dial is the new primary read.
2. **Constellation View (UI_SPEC §2i, NEW):** a growing visual of every successful orbital mission — Earth at center, one dot per successful satellite (Aurora I, Aurora II, fulfilled contracts), permanent and tappable for mission detail. Accessible from the Tracking Station tile and a small persistent ticker-area icon+count once it has ≥1 dot.
3. **Program Site Map (UI_SPEC §2h amended):** the Current Directive indicator (Sprint 9.5) gains a visual backing — a small evolving blueprint map of the whole facility, complexes in fixed relative positions, each building fading in as a pictogram (item 4) the instant it's built; unbuilt plots show as faint dashed outlines. Tap to expand full-screen.
4. **Building Pictograms (UI_SPEC §2 building-tile rule):** every building gets one consistent blueprint-style line icon, reused everywhere it appears (tile, Site Map, upgrade lists, tooltips) — single stroke weight, single accent color, no illustration. Exact glyph choices are Claude Code's call within the style; consistency (one mark per building, reused everywhere) is the actual requirement.
5. **Three cheap wins, no vote needed (near-zero risk, bundle in regardless):** staff panel role silhouette icons (abstract, no faces, role-colored accent marks — Technician/Engineer/Scientist/Controller); a weather-window radar-sweep visual replacing the bare countdown; Mission Log category icon badges (rocket/flask/people/building/document/star) for scannability.
**Acceptance:** the Launch Sequence screen's Confidence reads as an instrument, not a number; Tracking Station opens a Constellation View with a dot per real completed mission; the Site Map thumbnail is visible near the Current Directive and grows as buildings are built; every building tile carries its consistent icon across every screen it appears on (tile, map, upgrade lists). Playwright-verify all four render correctly at a few different game-state snapshots (early game with 1-2 buildings, mid-game, post-Aurora-II).

## Sprint 11 — Polish (3 days)
Rolling number animation, checklist transitions, **the full 10→0 visual countdown takeover (UI_SPEC screen 5/§1b — always spec'd, deferred from an earlier sprint, built now): full-screen sequence with narrative flavor lines, then the existing result card, per-tick sound and a distinct success sound**, **sound via Web Audio API synthesis only (UI_SPEC §1c) — no audio asset files in v1**, full narrative text review, accessibility pass, performance (tick must not re-render unchanged tiles — Zustand selectors). **ComplexTabs overflow fix:** horizontal overflow at ~420px viewport once 6 tabs are unlocked (found during Sprint 10.5, parked in BACKLOG) — this is its scheduled fix. **Contextual job titles:** staff slots display a per-building flavor title as secondary text (e.g. Finance: "Fundraising associate"; Launch Pad: "Pad crew") while the mechanical role stays visible — presentation only, roles/pools/data untouched; titles live in NARRATIVE_EVENTS (new §, referenced by ID). **Dedicated balance pass (SCOPED UNLOCK, this is its designated home):** with 10+ sprints of content now in, revisit the whole economy fresh — starting with the hiring-cost curve (BACKLOG: base cost and/or the 1.15^hiredOfThatRole factor feel too shallow for a program that will never have thousands of staff), the satellite-era Flight Data share (Sprint 10: jumped to 81-85% against the 20-35% target once Aurora II became repeatable — the target predates Aurora II existing, so this may be a metric that needs redefining rather than an economy problem: is unlimited late-game Research actually bad for a mature program with contracts/XP nodes still to spend it on, or does it just mean the 20-35% band should only apply to the pre-Aurora-II arc, with Aurora II's repeat-launch reward given diminishing returns instead if it does need a real fix? Answer with data, not assumption, before touching values), plus anything else the owner's own play has flagged as feeling off by this point. Full sim sweep (all three profiles) before and after; every sanity rule must hold. **Save-migration audit:** the schema has moved v1→v2→v3 across sprints; before calling v1 done, test loading a save from EVERY historical schema version, not just N-1→N — a broken old-save migration is one of the worst bugs to support once real testers have saves in the wild. **Cleanup pass:** confirm nothing marked `[v2]` (ECONOMY §4) leaked into any UI surface across the many additions since the original Sprint 7.5 audit; add Google Fonts attribution ahead of publishing; confirm the casual sim profile (added Sprint 8) has actually had its numbers reviewed at least once, not just instrumented and forgotten.

## Sprint 11.5 — Economy friction, progression depth & anti-slop audit (real-play findings, 14 items post-Sprint-11)
*Owner played the full post-Sprint-11 build and found: a real bug (salary burn not netted in the displayed rate), a real disclosure gap (R&D Lab appearing before it's usable), a real layout bug (Release button unclickable), a structural staffing decision (promotion-only Engineer/Scientist), and a cluster of "the economy is too linear/frictionless" findings that all point the same direction — plus a serious concern that the UI is reading as generic/AI-made. Two tracks, run together or in whichever order makes sense; Track B (anti-slop) should finish before Sprint 12 regardless of Track A's pace, since visual credibility matters for the first external eyes on this.*

**Track A — Economy friction & progression depth (SCOPED UNLOCK, sim-verify everything, propose-then-ratify per item, don't guess numbers):**
1. Real bug (fix immediately, no sim needed): displayed Funding/resource rates must net out salary burn (ECONOMY §3c) — "+20/s" should read "+19/s" if salary burn is -1/s.
2. Real bug: Release-staff button is covered by an adjacent element, uncancellable — fix the layout/z-order.
3. Engineer/Scientist made promotion-only (GDD §2 v2.11, ECONOMY §3 restructured) — implement the schema/UI change (no more direct-hire path for either role), repurpose `basicEngineering`/`scientificMethod` as promotion accelerators (ECONOMY §5, provisional −25% time/cost each, sim-verify).
4. Starting Funding cap: raise 500 → 1,000 (ECONOMY §1) — sim-verify pacing floor/ceiling still hold.
5. Second research track (BACKLOG's Campus/Production upgrade set): reprice to a real minimum (owner direction: ≥10,000 Funding + a genuine Materials cost) — it's the set's depth item and should feel earned.
6. Resource friction (ECONOMY §3b): design at least one real moment where staffing Fabrication/Refinery genuinely competes with the Materials/Propellant those hires consume. Propose specific lever(s) — raised consumption rates, tighter Supply Depot output relative to demand, etc. — sim-verify.
7. Research tree gating + mixed costs (ECONOMY §5b): (a) some nodes gain a building prerequisite, not just a tech one — Probe-1 engine research requiring Engine Test Stand built is the clear first case; propose others. (b) some nodes gain a secondary Funding/Materials cost alongside Research — not all, use judgment on which. (c) Research income needs rebalancing relative to node costs — 3 Scientists currently out-produce the entire remaining tree while waiting on a single >1h node; propose a fix (lower generation, raise costs, or both) and sim-verify against the 20-35%-per-era Flight Data band this doesn't disturb.
8. Hiring-cost curve — RE-OPEN, don't trust Sprint 11's prior "non-issue" conclusion as final (ECONOMY §3d): real play (aggressive single-building investment, e.g. leveling Finance hard early) directly contradicts it. Add an "aggressive-single-building" bot policy to the sim alongside optimal/human/casual and specifically test whether IT reproduces the "hiring feels trivial" experience before deciding whether to raise the curve.
9. Multi-resource building-upgrade costs at higher levels (ECONOMY §4d): past certain level thresholds, upgrades should require additional resource types (e.g. Finance: Funding-only to ~20, +Materials to ~50, +Hardware past that), varying per building. Propose thresholds/mixes, sim-verify.
10. Mid-game pacing check (Campus/Production complete → Testing unlocks): NOT a request to speed up research — verify the Current Directive (UI_SPEC §2h) correctly points toward the Test-stand tech path during this window, and confirm the Sprint 8/9.5 Campus-Production depth content (upgrades, building-expansion milestones) is actually landing as intended content for this stretch. Report findings; only add new content if data confirms a real gap, don't assume one.
11. VAB automation timing check: manual per-stage clicking is intentional (GDD pillar 5 — automation is earned), but check whether "VAB queues" (350R) and "Parallel integration" (700XP) currently unlock too late, making the manual stretch feel longer than intended. Report; adjust only with sim verification if genuinely too slow.

**Track B — Anti-slop visual audit (UI_SPEC §1d, presentation only, finish before Sprint 12):**
12. Read UI_SPEC §1d in full — new negative-constraint rules researched specifically to counter generic AI-generated UI patterns.
13. Audit every screen shipped since Sprint 1 against §1d's checklist: bordered cards where whitespace/background-shift should separate instead, any Hero-Metric-shaped tile, gradient text, off-accent colors, uniform (non-hierarchical) spacing, missing APCA contrast. Fix what's found.
14. R&D Lab reveal timing fix (UI_SPEC reveal table, item 3 above's disclosure half — belongs here since it's presentation, not economy).
15. Site Map: increase to its real specified minimum size (UI_SPEC §2h) — the shipped version registered as too small to have visual impact.
16. Manual-verb de-emphasis (UI_SPEC §4c): Pitch/Gather Materials/Rush Order recede visually (not removed) once their single use drops below ~1% of passive income in that resource, expanding back when relevant again.
17. Use the frontend-design skill's own guidance directly for every touch-up in this track, in addition to §1d's rules.

**Acceptance:** every Track A change is sim-verified (all sanity rules hold, or a specific deviation is reported and accepted deliberately); a fresh save shows R&D Lab appearing only after Classroom; the salary-net display bug and Release-button bug are gone; the Site Map registers visually on first glance; a genuine "would someone believe this is AI-made" self-check passes on every audited screen; Playwright-verify the disclosure and layout fixes live.

## Sprint 12 — Web playtest (public)
0. **Owner's own "cold" playtest, before external testers or deployment:** install the build, don't touch dev tools (no time-warp, no reset), and leave it running across real elapsed days like an actual player would. Every prior playtest session has been the owner's own concentrated, tool-assisted play — this is the only way to feel the real offline-return experience before anyone else does. Itch.io deployment was deliberately deferred to this sprint; this step should happen before that deployment, not after.
1. Deploy (itch.io + Vercel), feedback form.
2. **Telemetry decision (default: minimal remote endpoint):** a single Vercel serverless function receiving anonymous batched funnel events (opt-in checkbox in Settings, disclosed plainly). Without a remote sink, D1/D7 retention CANNOT be measured — if the endpoint is cut, delete the retention benchmarks from this sprint and treat the playtest as qualitative-only. No third-party analytics SDKs in v1.
3. **Distribution:** post to r/incremental_games (the genre's home community — feedback there is expert-level), itch.io devlog, incremental-games forums/Discords. One honest "solo dev, first playable, brutal feedback welcome" post outperforms any ad.
4. Funnel review + retention read. NOTE on benchmarks: D1 35–40% / D7 10–15% are generic mobile-F2P figures and are the WRONG comparison for a web idle game reaching a self-selected enthusiast audience via itch.io and r/incremental_games. Treat them as directional only; the actionable signals are the funnel drop-off points, session length, and whether returning players come back on day 2–3 at all.
Android + monetization are a SEPARATE post-launch phase, planned only after reading playtest data.
